
mqtt:
  # hostname of the MQTT broker
  broker: bpi1.fritz.box
  
  # Todo use hostname
  node_name: rpi2

  # MQTT topic prefix
  mqtt_topic_prefix: /sensors


sensors:
  loadavg:
    sensor_class: sensors.LoadAvg.LoadAvg

  cputemp:
    sensor_class: sensors.RPiInternalTemp.RPiInternalTemp
  
  room:
    sensor_class: sensors.w1therm.W1ThermSensor
    w1_id: "10-0008029061ff"
        
  outside:
    sensor_class: sensors.w1therm.W1ThermSensor
    w1_id: "10-0008028d5e47"

  